import React from 'react';
import PropTypes from 'prop-types'



const About = (props) => {
        return (
            <div>Signed out</div>
        );
}

About.propTypes = {
    text: PropTypes.string
}

export default About;
